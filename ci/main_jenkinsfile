pipeline {
    agent none

    options {
        timestamps()
    }

    parameters{
        choice choices: ['Release', 'Debug'], description: '', name: 'BUILD_TYPE'
    }

    environment {
        PROJECT_NAME = "milvus"
        LOWER_BUILD_TYPE = params.BUILD_TYPE.toLowerCase()
    }

    stages {
        stage("Ubuntu 16.04") {
            stages {
                stage("Run Build") {
                    agent {
                        kubernetes {
                            label 'build'
                            defaultContainer 'milvus-build-env'
                            yamlFile 'ci/pod_containers/milvus-build-env-pod.yaml'
                        }
                    }
                    stages {
                        stage('Build') {
                            steps {
                                sh 'echo "Build"'
                                sh("printenv")
                            }
                        }
                    }
                }

                stage("Publish docker and helm") {
                    agent {
                        kubernetes {
                            label 'publish'
                            defaultContainer 'publish-docker'
                            yamlFile 'ci/pod_containers/docker-pod.yaml'
                        }
                    }

                    stages {
                        stage('Publish Docker') {
                            steps {
                                sh 'echo "Publish Docker"'
                                sh("printenv")
                                def jobName = sh(script: "echo ${JOB_NAME%/}", returnStdout: true)
                                sh "echo ${jobName}"
                            }
                        }
                    }
                }
            }
        }
    }
}

